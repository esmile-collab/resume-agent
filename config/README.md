<!--
Input: config 目录结构、子目录边界和文件职责清单。
Output: 输出当前目录的极简架构说明与成员地图。
Pos: config 的目录说明文件。
Rule: 一旦我被更新，务必同步更新本文件头注释与所属目录 README。
-->
# config

- 定位：仓库默认配置基线，只存环境无关参数。
- 边界：服务启动前读取这里的默认值，不承载业务编排。
- 维护：本目录文件变更后，先更新本页，再检查根目录 README 的系统地图。

## 文件清单

| 文件 | 地位 | 功能 |
| --- | --- | --- |
| `README.md` | 目录地图 | 解释当前目录下有哪些成员以及它们各自做什么。 |
| `default.yaml` | 默认配置 | 定义本地开发的基础参数，不处理业务逻辑。 |

## 子目录

| 子目录 | 地位 | 功能 |
| --- | --- | --- |
| `（空）` | 叶子目录 | 当前目录没有下一级目录。 |
