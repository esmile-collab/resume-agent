# 验收检查清单

本文档是测试 Agent 进行验收检查的清单模板。

---

## 模块验收检查清单 - M<模块编号>

### 基本信息

| 项目 | 内容 |
|---|---|
| **模块名称** | |
| **开发 Agent** | |
| **测试时间** | |
| **Git Commit** | |
| **分支名称** | |

---

### 1. 环境检查

- [ ] 创建独立测试环境
- [ ] 安装依赖成功
- [ ] 数据库初始化成功

**测试环境**: `.venv_test_<timestamp>`
**Python 版本**:

---

### 2. 功能验收

#### 2.1 核心功能

| 功能点 | 测试命令/方法 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| | | | | |
| | | | | |

#### 2.2 边界情况

| 场景 | 测试命令/方法 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| | | | | |

#### 2.3 错误处理

| 错误类型 | 触发方式 | 预期处理 | 实际处理 | 状态 |
|---|---|---|---|---|
| | | | | |

---

### 3. 接口验收

#### 3.1 数据结构

```python
# 检查数据结构定义
from src.db.models import Project

# 必需字段
assert hasattr(Project, 'id')
assert hasattr(Project, 'name')
assert hasattr(Project, 'created_at')
```

- [ ] 所有必需字段存在
- [ ] 类型注解正确
- [ ] Docstring 完整

#### 3.2 函数签名

```python
# 检查函数签名
from src.db.crud import ProjectCRUD
import inspect

sig = inspect.signature(ProjectCRUD.create)
params = list(sig.parameters.keys())
assert params == ['self', 'name']
```

- [ ] 参数列表正确
- [ ] 返回类型正确
- [ ] 默认值合理

---

### 4. 测试覆盖率

```
名称              Stmts   Miss  Cover   Missing---------------------------------------
src/db/crud.py        25      2    92%   23-24
src/db/models.py      10      0   100%
src/db/database.py    15      3    80%   45-47
------------------------------------------------------------------
TOTAL                 50      5    90%
```

- [ ] 整体覆盖率 > 80%
- [ ] 核心模块覆盖率 > 90%

---

### 5. 代码质量

#### 5.1 代码格式

```bash
$ black --check src/
All done! ✨ 🍰 ✨
```

- [ ] black 格式检查通过

#### 5.2 类型检查

```bash
$ mypy src/
Success: no issues found in 5 source files
```

- [ ] mypy 类型检查通过

#### 5.3 导入检查

```bash
$ python -m py_compile src/**/*.py
```

- [ ] 所有模块可正常导入

---

### 6. 性能检查

| 操作 | 预期耗时 | 实际耗时 | 状态 |
|---|---|---|---|
| 创建项目 | < 0.1s | | |
| 查询项目 | < 0.05s | | |
| 列出项目 | < 0.1s | | |

---

### 7. 文档检查

- [ ] README 存在且完整
- [ ] API 文档更新
- [ ] 示例代码可运行
- [ ] 变更日志记录

---

### 8. 依赖检查

```bash
$ pip list | grep -E "(依赖包名)"
```

- [ ] 无多余依赖
- [ ] 版本号合理
- [ ] 安全漏洞检查通过

---

## 验收结果

### 统计

| 项目 | 数量 |
|---|---|
| 总测试用例 | |
| 通过 | |
| 失败 | |
| 跳过 | |
| 覆盖率 | |

### 决策

- [ ] **通过** - 所有验收项通过，可以合并
- [ ] **条件通过** - 存在非阻塞性问题，可以合并但需要后续修复
- [ ] **不通过** - 存在阻塞性问题，需要回滚修复

### 详细评价

**优点**:
1.
2.
3.

**问题**:
1.
2.
3.

**建议**:
1.
2.
3.

---

## 附录

### 测试报告位置

```
test_reports/<timestamp>/
├── report.html          # pytest HTML 报告
├── coverage.xml         # 覆盖率报告
├── junit.xml            # JUnit 格式报告
└── summary.txt          # 测试摘要
```

### 回滚命令（如需要）

```bash
# 非破坏性回滚：创建回滚分支
git checkout -b rollback-<timestamp> <上一个稳定版本>
# 可选：标记失败的 tag
git tag -a <当前版本>-failed -m "验收测试失败"
```

### 问题跟踪

| 问题ID | 描述 | 严重程度 | 状态 |
|---|---|---|---|
| | | | |

---

**测试 Agent 签名**: __________________
**日期**: __________________
