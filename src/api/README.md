<!--
Input: src/api 目录结构、子目录边界和文件职责清单。
Output: 输出当前目录的极简架构说明与成员地图。
Pos: src/api 的目录说明文件。
Rule: 一旦我被更新，务必同步更新本文件头注释与所属目录 README。
-->
# src/api

- 定位：FastAPI HTTP 入口层。
- 边界：把前端与外部调用转成运行时、记忆管理和产物管理操作。
- 维护：新增端点前先确认它属于现有 session/message 模型。

## 文件清单

| 文件 | 地位 | 功能 |
| --- | --- | --- |
| `README.md` | 目录地图 | 解释当前目录下有哪些成员以及它们各自做什么。 |
| `__init__.py` | 包声明 | 保持 API 包可被应用服务器导入。 |
| `main.py` | HTTP 入口 | 承接前端请求并转发到 runtime 与 memory。 |

## 子目录

| 子目录 | 地位 | 功能 |
| --- | --- | --- |
| `（空）` | 叶子目录 | 当前目录没有下一级目录。 |
