<!--
Input: src 目录结构、子目录边界和文件职责清单。
Output: 输出当前目录的极简架构说明与成员地图。
Pos: src 的目录说明文件。
Rule: 一旦我被更新，务必同步更新本文件头注释与所属目录 README。
-->
# src

- 定位：后端源码主目录。
- 边界：按运行时、API、数据库、评分、服务与观测分层组织。
- 维护：任何模块边界调整都必须先更新这里，再更新对应子目录 README。

## 文件清单

| 文件 | 地位 | 功能 |
| --- | --- | --- |
| `README.md` | 目录地图 | 解释当前目录下有哪些成员以及它们各自做什么。 |
| `__init__.py` | 包声明 | 提供源码包的最外层命名空间。 |

## 子目录

| 子目录 | 地位 | 功能 |
| --- | --- | --- |
| `agent` | 子模块目录 | 会话式 Agent 核心编排层。 |
| `api` | 子模块目录 | FastAPI HTTP 入口层。 |
| `cli` | 子模块目录 | 命令行入口层。 |
| `db` | 子模块目录 | SQLite 持久化层。 |
| `observability` | 子模块目录 | 轻量观测与对话压缩目录。 |
| `scoring` | 子模块目录 | 评分引擎目录。 |
| `services` | 子模块目录 | 领域服务目录。 |
| `tools` | 子模块目录 | 工具命名空间占位目录。 |
