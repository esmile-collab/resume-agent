<!--
Input: src/cli 目录结构、子目录边界和文件职责清单。
Output: 输出当前目录的极简架构说明与成员地图。
Pos: src/cli 的目录说明文件。
Rule: 一旦我被更新，务必同步更新本文件头注释与所属目录 README。
-->
# src/cli

- 定位：命令行入口层。
- 边界：提供数据库初始化、服务启动和会话调试命令。
- 维护：CLI 能力要和 HTTP 入口保持同一套运行时语义。

## 文件清单

| 文件 | 地位 | 功能 |
| --- | --- | --- |
| `README.md` | 目录地图 | 解释当前目录下有哪些成员以及它们各自做什么。 |
| `__init__.py` | 包声明 | 保持 CLI 模块可被 setuptools 命令发现。 |
| `main.py` | CLI 入口 | 为本地启动和会话调试提供命令接口。 |

## 子目录

| 子目录 | 地位 | 功能 |
| --- | --- | --- |
| `（空）` | 叶子目录 | 当前目录没有下一级目录。 |
