<!--
Input: src/db 目录结构、子目录边界和文件职责清单。
Output: 输出当前目录的极简架构说明与成员地图。
Pos: src/db 的目录说明文件。
Rule: 一旦我被更新，务必同步更新本文件头注释与所属目录 README。
-->
# src/db

- 定位：SQLite 持久化层。
- 边界：定义表结构、数据库连接和 CRUD 仓储，为 runtime 提供状态落盘。
- 维护：表结构变化时，必须同步更新技术文档里的对象模型。

## 文件清单

| 文件 | 地位 | 功能 |
| --- | --- | --- |
| `README.md` | 目录地图 | 解释当前目录下有哪些成员以及它们各自做什么。 |
| `__init__.py` | 包声明 | 保持 db 包命名空间稳定。 |
| `agent_crud.py` | 持久化仓储 | 服务 sessions、messages、tracks、artifacts 等核心对象。 |
| `crud.py` | 持久化仓储 | 管理 project 与 project_jd_entries 等基础实体。 |
| `database.py` | 数据库入口 | 保证持久化目录和表结构随服务启动可用。 |
| `models.py` | 实体模型 | 承接 CRUD 返回值和类型约束。 |

## 子目录

| 子目录 | 地位 | 功能 |
| --- | --- | --- |
| `（空）` | 叶子目录 | 当前目录没有下一级目录。 |
